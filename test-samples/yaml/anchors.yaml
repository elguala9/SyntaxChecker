defaults: &defaults
  adapter: postgres
  host: localhost
  pool: 5

development:
  <<: *defaults
  database: app_development

test:
  <<: *defaults
  database: app_test

# explicit standard tags stay valid in strict mode
port: !!int "8080"
debug: !!bool "true"
