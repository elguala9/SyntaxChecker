# Application configuration
title = "Syntax Checker"

[server]
host = "0.0.0.0"
port = 8080
enabled = true

[database]
urls = ["postgres://a", "postgres://b"]
timeout = 30.5

[[worker]]
name = "alpha"

[[worker]]
name = "beta"
